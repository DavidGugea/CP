#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);

	int n; cin >> n;
	vector<int> v;
	int val;

	for (int i = 0 ; i < n; ++i) {
		cin >> val;
		v.push_back(val);
	}

	return 0;
}